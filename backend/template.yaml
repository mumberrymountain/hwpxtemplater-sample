AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: >
  backend

  Sample SAM Template for backend

# More info about Globals: https://github.com/awslabs/serverless-application-model/blob/master/docs/globals.rst
Globals:
  Function:
    Timeout: 20
    Runtime: java17
    Architectures:
      - x86_64
    MemorySize: 512
    CodeUri: functions
    SnapStart:
      ApplyOn: PublishedVersions
    AutoPublishAlias: live

Resources:
  BasicApi:
    Type: AWS::Serverless::Api
    Properties:
      StageName: Prod
      BinaryMediaTypes:
        - application/hwp+zip
        - application/octet-stream
      Auth:
        ApiKeyRequired: false
      Cors:
        AllowOrigin: "'https://www.hwpxtemplater.link'"
        AllowHeaders: "'Content-Type,Authorization'"
        AllowMethods: "'GET,POST,OPTIONS'"
        AllowCredentials: false
  # API 1: Basic
  BasicFunction:
    Type: AWS::Serverless::Function
    Properties:
      Handler: com.mumberrymountain.BasicHandler::handleRequest
      ProvisionedConcurrencyConfig:
        ProvisionedConcurrentExecutions: 1
      Events:
        BasicApiEvent:
          Type: Api
          Properties:
            RestApiId: !Ref BasicApi
            Path: /basic
            Method: post
        BasicApiOptionsEvent:
          Type: Api
          Properties:
            RestApiId: !Ref BasicApi
            Path: /basic
            Method: options

  # API 2: Condition
  ConditionFunction:
    Type: AWS::Serverless::Function
    Properties:
      Handler: com.mumberrymountain.ConditionHandler::handleRequest
      Events:
        ConditionApi:
          Type: Api
          Properties:
            RestApiId: !Ref BasicApi
            Path: /condition
            Method: post
        ConditionApiOptionsEvent:
          Type: Api
          Properties:
            RestApiId: !Ref BasicApi
            Path: /condition
            Method: options

  # API 3: Loop
  LoopFunction:
    Type: AWS::Serverless::Function
    Properties:
      Handler: com.mumberrymountain.LoopHandler::handleRequest
      Events:
        LoopApi:
          Type: Api
          Properties:
            RestApiId: !Ref BasicApi
            Path: /loop
            Method: post
        LoopApiOptionsEvent:
          Type: Api
          Properties:
            RestApiId: !Ref BasicApi
            Path: /loop
            Method: options

  # API 4: Image
  ImageFunction:
    Type: AWS::Serverless::Function
    Properties:
      Handler: com.mumberrymountain.ImageHandler::handleRequest
      Events:
        ImageApi:
          Type: Api
          Properties:
            RestApiId: !Ref BasicApi
            Path: /image
            Method: post
        ImageApiOptionsEvent:
          Type: Api
          Properties:
            RestApiId: !Ref BasicApi
            Path: /image
            Method: options

  # API 5: Table
  TableFunction:
    Type: AWS::Serverless::Function
    Properties:
      Handler: com.mumberrymountain.TableHandler::handleRequest
      Events:
        TableApi:
          Type: Api
          Properties:
            RestApiId: !Ref BasicApi
            Path: /table
            Method: post
        TableApiOptionsEvent:
          Type: Api
          Properties:
            RestApiId: !Ref BasicApi
            Path: /table
            Method: options

  # API 1: Basic
  TemplateFunction:
    Type: AWS::Serverless::Function
    Properties:
      Handler: com.mumberrymountain.TemplateHandler::handleRequest
      Events:
        BasicApiEvent:
          Type: Api
          Properties:
            RestApiId: !Ref BasicApi
            Path: /template
            Method: post
        TemplateApiOptionsEvent:
          Type: Api
          Properties:
            RestApiId: !Ref BasicApi
            Path: /template
            Method: options

Outputs:
  ApiGatewayUrl:
    Description: "API Gateway Base URL"
    Value: !Sub "https://${BasicApi}.execute-api.${AWS::Region}.amazonaws.com/Prod"
  
  BasicUrl:
    Description: "Basic Function URL"
    Value: !Sub "https://${BasicApi}.execute-api.${AWS::Region}.amazonaws.com/Prod/basic"

  ConditionUrl:
    Description: "Condition Function URL"
    Value: !Sub "https://${BasicApi}.execute-api.${AWS::Region}.amazonaws.com/Prod/condition"

  LoopUrl:
    Description: "Loop Function URL"
    Value: !Sub "https://${BasicApi}.execute-api.${AWS::Region}.amazonaws.com/Prod/loop"

  ImageUrl:
    Description: "Image Function URL"
    Value: !Sub "https://${BasicApi}.execute-api.${AWS::Region}.amazonaws.com/Prod/image"

  TableUrl:
    Description: "Table Function URL"
    Value: !Sub "https://${BasicApi}.execute-api.${AWS::Region}.amazonaws.com/Prod/table"

  TemplateUrl:
    Description: "Template Function URL"
    Value: !Sub "https://${BasicApi}.execute-api.${AWS::Region}.amazonaws.com/Prod/template"
